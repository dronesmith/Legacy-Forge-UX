'use strict';

angular
  .module('ForgeApp')
  .controller('LoginViewCtrl', function(
    $scope,
    Session
  ) {

    // Session
    //   .get({}, function(data) {
    //     $scope.userInfo = data.userData || null;
    //     if ($scope.userInfo) {
    //       ga('set', '&uid', $scope.userInfo.id);
    //       $state.go('forge');
    //     }
    //   })
    // ;
  })
;
